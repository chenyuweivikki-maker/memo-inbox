# 灵感收件箱

打字/粘贴内容 → AI 自动分类（自媒体选题/好句/小说灵感/摘录/Todo/其他）→ 自动归档为带日期的 Markdown 文件。

## 启动

```bash
cd ~/memo-app
python3 -m uvicorn app:app --host 0.0.0.0 --port 8787
```

- 电脑浏览器打开：http://localhost:8787
- 手机（同一 WiFi）打开：http://192.168.1.13:8787

## 目录结构

```
memo-app/
├── app.py              # 后端：接收内容→调AI分类→存文件
├── categories.json     # 分类列表（可管理：添加/改名/排序/删除）
├── static/
│   └── index.html      # 前端页面（极简黑白风，手机端）
└── memos/              # 数据（按分类分文件夹，文件名带日期）
    ├── 日记/
    ├── 自媒体选题/
    ├── 好句/
    ├── 小说灵感/
    ├── 摘录/
    ├── Todo/
    └── 其他/
```

## 使用

1. 首页输入框打字或粘贴内容（支持中文）
2. 点"丢进去"（或 Cmd+Enter）
3. 等 2-3 秒，自动分类归档（失败自动重试2次）
4. 左侧分类栏筛选查看（手机端点左上角汉堡展开），按日期倒序
5. 首页带一只黑白色线条小猫动态图
6. 每条内容右下角有"删除"按钮（带确认，防误删）
7. 侧边栏底部"导出当前分类"按钮：在"全部"视图=导出全部，在某分类视图=只导出该分类（zip 文件）
8. 每条卡片左下角只显示日期+时间（灰色小字）
9. "+ 添加分类"按钮：添加新分类
10. 右键（电脑）或长按（手机）分类项：弹出菜单，可改名 / 上移 / 下移 / 删除分类（改名时数据自动迁移）

## 技术说明

- 后端：Python FastAPI + uvicorn
- AI 分类：调用你配置的 ccsub.net 中转（claude-haiku-4-5）
- 数据：Markdown 文件，可随时迁移到 Obsidian
- 无数据库、无外部依赖（除 FastAPI/uvicorn）

## 已实现

- ✅ 文字/粘贴输入
- ✅ AI 自动分类（6 大类：选题/好句/灵感/摘录/Todo/其他）
- ✅ 分类失败自动重试（最多2次）
- ✅ 自动打主题标签
- ✅ 文件名带日期
- ✅ 左侧分类栏筛选（手机端抽屉式），按日期倒序
- ✅ 极简黑白风 UI，首页只有输入框
- ✅ 首页黑白色线条小猫动态图（SVG）
- ✅ 手机同 WiFi 可访问
- ✅ 每条内容可删除（带确认）
- ✅ 导出 zip：全部 / 单分类（侧边栏"导出当前分类"按钮）
- ✅ 分类可管理：添加（"+ 添加分类"）、改名/排序/删除（右键或长按分类项弹出菜单）
- ✅ 分类存储在 categories.json，AI 自动分类跟随分类列表动态变化

## 后续可加

- 语音输入（手机录音 → 转文字）
- 子标签细分（哪本小说、什么主题）
- 部署到云端（随时手机访问，不依赖电脑开机）
- 搜索功能
